# 部署变更清单

**生成时间**: 2026-05-28 20:35
**远程版本**: 9aea71bfb2530d4cb8e4943813dd24b94ea50cb1
**本地版本**: 660ac7a99d06aeb85d3e0107dcac26675a3ab966
**涉及提交数**: 9

## 提交列表

- `660ac7a` feat(gallery, mainwave): add gallery module and refine mainwave picker v2
- `9b0c495` fix(frontend): fix MainwavePicker symbol filter not sent to backend
- `4d91657` fix(frontend): remove trailing commas causing ESLint errors
- `7161a07` feat(mainwave): scoring system v2.0 with multi-dimensional rating
- `61a3ff6` feat: agent chat, auth system, board pages, system logs, and performance optimizations
- `cc7e512` feat: add sync status dashboard, portfolio daily tracking, market sentiment and mainwave picker
- `34f22f0` feat(portfolio): add daily-pnl debug endpoint
- `9ef2654` feat(pagination): server-side pagination for daily data by date

## 变更文件汇总

### 新增文件 (A) - 后端
- `seele-backend/app/routes/gallery.py` — 图库管理路由（上传/列表/删除）

### 新增文件 (A) - 数据库迁移
- `db-ops/migrations/2026-05-28-add-portfolio-trade-dividend.sql` — portfolio_trade 新增 dividend 字段

### 修改文件 (M) - 后端
- `seele-backend/app/models.py` — 新增 GalleryImage 模型、adx 字段；**工作区新增 PortfolioTrade.dividend**
- `seele-backend/app/schemas.py` — 新增 GalleryImageResponse；**工作区新增 PortfolioTrade 相关 schema 的 dividend**
- `seele-backend/app/routes/portfolio.py` — **工作区变更：清仓盈亏与历史盈亏计算纳入分红**
- `seele-backend/app/crud.py` — MainwavePicker 过滤条件优化
- `seele-backend/app/main.py` — 注册 gallery 路由、挂载 uploads 静态文件
- `seele-backend/app/schemas.py` — 新增 GalleryImageResponse
- `seele-backend/app/services/mainwave_scorer.py` — 评分逻辑调整
- `seele-backend/app/routes/stock_daily.py` — 接口调整
- `seele-backend/app/routes/stock_indicator.py` — 接口调整
- `seele-backend/app/scheduler_jobs.py` — 定时任务调整
- `seele-backend/app/routes/sync.py` — 同步逻辑调整
- `seele-backend/app/config.py` — 仅换行符变化，无实质变更
- `seele-backend/requirements.txt` — 仅换行符变化，无实质变更
- `seele-backend/app/database.py` — 调整
- `seele-backend/start.py` — 调整
- `seele-backend/app/__init__.py` — 调整
- `seele-backend/docs/API.md` — 文档更新
- `seele-backend/scripts/sync_history_data.py` — 调整

### 修改文件 (M) - 数据库迁移
- `db-ops/migrations/2026-05-07-add-portfolio-position-snapshot.sql` — 内容调整

### 新增文件 (A) - 前端
- `seele-frontend/src/api/gallery.js` — 图库 API 客户端
- `seele-frontend/public/gallery-images.json` — 图库静态数据
- `seele-frontend/public/gallery/*.png` / `*.jpg` — 图库示例图片（11 张）
- `seele-frontend/scripts/kill-port.js` — 端口清理脚本

### 修改文件 (M) - 前端
- `seele-frontend/src/views/GalleryView.vue` — 图库页面大幅重构
- `seele-frontend/src/views/HomeGalleryView.vue` — 首页图库调整
- `seele-frontend/src/views/MainwavePickerView.vue` — 主浪选股调整
- `seele-frontend/src/components/stock/MainwavePickerFilter.vue` — 过滤器调整
- `seele-frontend/src/components/portfolio/PortfolioTradeModal.vue` — **工作区变更：表单改为双列布局，卖出时新增分红输入框**

## 关键变更分析

### 数据库相关（需特别注意）

**变更 1: `stock_daily_indicator` 表新增 `adx` 字段**
- models.py 中 `adx = Column(Float, comment="ADX趋势强度")`
- **⚠️ 无对应迁移脚本** — 需手动 ALTER：
  ```sql
  ALTER TABLE stock_daily_indicator ADD COLUMN adx FLOAT COMMENT 'ADX趋势强度' AFTER boll_lower;
  ```

**变更 2: 新增 `gallery_image` 表**
- models.py 中新增 `GalleryImage` 模型
- **⚠️ 无对应迁移脚本** — 需手动建表：
  ```sql
  CREATE TABLE IF NOT EXISTS gallery_image (
      id INT AUTO_INCREMENT PRIMARY KEY,
      filename VARCHAR(255) NOT NULL COMMENT '存储文件名',
      original_name VARCHAR(255) NOT NULL COMMENT '原始文件名',
      file_size INT NOT NULL COMMENT '文件大小(字节)',
      mime_type VARCHAR(50) NOT NULL COMMENT 'MIME类型',
      url_path VARCHAR(500) NOT NULL COMMENT '访问路径',
      created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间'
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='图库图片表';
  ```

**变更 3: `portfolio_trade` 表新增 `dividend` 字段**
- models.py 中 `dividend = Column(Float, default=0, comment='分红金额')`
- **✅ 已有对应迁移脚本** — `db-ops/migrations/2026-05-28-add-portfolio-trade-dividend.sql`：
  ```sql
  ALTER TABLE portfolio_trade
  ADD COLUMN dividend FLOAT DEFAULT 0 COMMENT '分红金额' AFTER fee;
  ```
- 清仓盈亏和历史盈亏计算已纳入分红（`realized_pnl = total_sell_amount - total_buy_amount - total_fee + total_dividend`）

**变更 4: `db-ops/migrations/2026-05-07-add-portfolio-position-snapshot.sql` 被修改**
- 需确认远程是否已执行旧版本，如已执行则只需检查差异部分

**远程已存在的迁移脚本（无需重复执行）**
以下迁移脚本在本地和远程内容一致，说明远程已部署：
- `2026-05-09-add-market-sentiment-tables.sql`
- `2026-05-09-add-portfolio-daily-tables.sql`
- `2026-05-09-add-portfolio-daily-pnl.sql`
- `2026-05-12-add-indicator-chg-columns.sql`
- `2026-05-16-add-board-tables.sql`
- `2026-05-20-add-system-log-tables.sql`
- `2026-05-25-add-performance-indexes.sql`
- `2026-05-25-dedup-stock-basic.sql`

### API / 接口相关

- **新增 `/api/gallery/*` 路由**：图片上传(POST)、列表(GET)、删除(DELETE)
- **新增 `/uploads` 静态文件挂载**：图库图片通过 `/uploads/gallery/<filename>` 访问
- **MainwavePicker 查询逻辑调整**：移除市值过滤、修复 amount 单位转换（前端亿元→数据库元）
- **评分逻辑调整**：mainwave_scorer v2 评分维度微调

### 前端 UI 相关

- **GalleryView.vue 大幅重构**：新增图库管理功能（上传、预览、删除）
- **HomeGalleryView.vue 调整**：配合图库功能更新
- **MainwavePickerView.vue / MainwavePickerFilter.vue**：修复 symbol 过滤未传后端、过滤条件调整

### 配置 / 部署相关

- `seele-backend/app/config.py` — 仅换行符变化（CRLF/LF），无实质变更
- `seele-backend/requirements.txt` — 仅换行符变化，无实质变更
- `deploy/setup-2c2g.sh` — 调整
- `docs/deployment-optimization.md` — 调整

## 部署检查项

- [ ] **数据库结构更新**
  - [ ] 执行 `ALTER TABLE stock_daily_indicator ADD COLUMN adx FLOAT...`
  - [ ] 执行 `CREATE TABLE gallery_image...`
  - [ ] 执行 `ALTER TABLE portfolio_trade ADD COLUMN dividend FLOAT...`
  - [ ] 检查 `2026-05-07-add-portfolio-position-snapshot.sql` 是否需要更新
- [ ] **后端依赖** — requirements.txt 无实质变化，无需重新安装
- [ ] **前端构建** — 需要重新 `npm run build`（Vue CLI 项目）
- [ ] **Nginx 配置** — 需要确保 `/uploads` 路径可访问（后端已挂载 StaticFiles）
- [ ] **Systemd 服务** — 无变更，无需 reload
- [ ] **环境变量** — 无新增项
- [ ] **远程 uploads 目录** — 确保 `/www/seele/seele-backend/uploads/gallery` 存在且有写入权限

## 远程手动操作备忘

```bash
# 1. 登录远程 MySQL，执行数据库变更
mysql -u root -p seele

# 2. 添加 adx 字段
ALTER TABLE stock_daily_indicator
ADD COLUMN adx FLOAT COMMENT 'ADX趋势强度' AFTER boll_lower;

# 3. 创建 gallery_image 表
CREATE TABLE IF NOT EXISTS gallery_image (
    id INT AUTO_INCREMENT PRIMARY KEY,
    filename VARCHAR(255) NOT NULL COMMENT '存储文件名',
    original_name VARCHAR(255) NOT NULL COMMENT '原始文件名',
    file_size INT NOT NULL COMMENT '文件大小(字节)',
    mime_type VARCHAR(50) NOT NULL COMMENT 'MIME类型',
    url_path VARCHAR(500) NOT NULL COMMENT '访问路径',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='图库图片表';

# 4. 添加 portfolio_trade.dividend 字段
ALTER TABLE portfolio_trade
ADD COLUMN dividend FLOAT DEFAULT 0 COMMENT '分红金额' AFTER fee;

# 5. 确保 uploads 目录存在且有写入权限
mkdir -p /www/seele/seele-backend/uploads/gallery
chmod 755 /www/seele/seele-backend/uploads/gallery
```

## 未提交变更（工作区）

本次变更尚未形成 git commit，属于当前工作区的修改：

### 后端
- `seele-backend/app/models.py` — `PortfolioTrade` 新增 `dividend` 字段
- `seele-backend/app/schemas.py` — `PortfolioTradeCreate` / `PortfolioTradeUpdate` / `PortfolioTradeResponse` 新增 `dividend`
- `seele-backend/app/routes/portfolio.py` — 清仓盈亏与历史盈亏计算纳入分红

### 前端
- `seele-frontend/src/components/portfolio/PortfolioTradeModal.vue` — 表单改为双列布局，卖出时新增「分红」输入框

### 数据库迁移
- `db-ops/migrations/2026-05-28-add-portfolio-trade-dividend.sql` — 新增迁移脚本

## 高风险项标记

- **🔴 models.py 有变更但缺少迁移脚本**：`adx` 字段和 `gallery_image` 表均无 `.sql` 迁移文件，必须手动执行 ALTER/CREATE
