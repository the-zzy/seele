# 部署变更清单 — v2.0 → v2.1

**生成时间**: 2026-06-08 02:30
**远程版本**: `6fbd16b` (deploy: 2026-05-28-20:51)
**本地版本**: `0c13f65` (chore: bump version to 2.1.0...)
**涉及提交数**: 35（含 v2.0 完整历史 + 2 个 v2.1 新增提交）

> 注：远程 `production/master` 是独立 git 历史（从代码快照初始化），
> 本次部署实质是 v2.0 正式部署 + v2.1 增量升级的**全量推送**。

---

## v2.1 新增提交（相对于 v2.0）

```
0c13f65 chore: bump version to 2.1.0 and update sync log view layout
ac2b462 feat(scheduler): add board/ETF daily sync scheduled task
5f7a8a8 fix(models, migrations, ci): resolve AI review issues
```

## 变更文件汇总（v2.1 增量）

### 新增文件 (A)
- `seele-backend/scripts/sync_board_constituent.py` — 板块成分股同步脚本
- `db-ops/migrations/2026-06-08-board-constituent-name.sql` — 成分股 name 字段迁移

### 修改文件 (M) — 后端
- `seele-backend/app/__init__.py` — 版本号 `1.0.0` → `2.1.0`
- `seele-backend/app/models.py` — 小改（字段调整、索引优化）
- `seele-backend/app/schemas.py` — Schema 调整
- `seele-backend/app/crud.py` — +107/-0 行
- `seele-backend/app/main.py` — 路由注册调整
- `seele-backend/app/routes/board.py` — **新增板块路由**
- `seele-backend/app/routes/sync.py` — 同步逻辑大改（+398/-189）
- `seele-backend/app/routes/portfolio.py` — 新增分红、日交易接口（+71）
- `seele-backend/app/routes/market_sentiment.py` — 重构
- `seele-backend/app/routes/pickers.py` — 选股器调整
- `seele-backend/app/scheduler_jobs.py` — 新增板块/ETF 日线定时同步（+48/-0）
- `seele-backend/app/sync_worker.py` — 同步 worker 优化（+71）
- `seele-backend/app/services/mainwave_detector.py` — **新增主浪检测服务**
- `seele-backend/app/services/mainwave_scorer.py` — 评分逻辑微调
- `seele-backend/app/agent/context.py` — Agent 上下文调整
- `seele-backend/app/agent/tools.py` — Agent 工具精简（-45）

### 修改文件 (M) — 前端
- `seele-frontend/package.json` — 版本号 `0.1.0` → `2.1.0`
- `seele-frontend/src/App.vue` — 页脚布局重构、v2.1 版本号
- `seele-frontend/src/views/SyncJobLogView.vue` — 同步日志页面重构：板块状态卡片集成到左侧列
- `seele-frontend/src/views/BoardListView.vue` — 修复 ESLint 尾部逗号（构建时修复）
- `seele-frontend/src/composables/useSyncTask.js` — 同步任务 composable 调整
- `seele-frontend/src/api/portfolio.js` — 新增分红 API 调用
- `seele-frontend/src/api/stock.js` — API 调用调整
- `seele-frontend/src/router/index.js` — 路由调整（板块相关）
- `seele-frontend/src/components/portfolio/PortfolioTradeTable.vue` — 交易列表调整
- `seele-frontend/src/components/common/ConfirmDialog.vue` — **新增确认弹窗组件**
- `seele-frontend/src/components/portfolio/PortfolioDayTradeModal.vue` — **新增日交易弹窗组件**
- `seele-frontend/src/utils/formatters.js` — 格式化工具调整

### 其他
- `package.json` — 版本号 `1.0.0` → `2.1.0`
- `AGENTS.md` / `SKILLS.md` — 项目文档更新

---

## 关键变更分析

### 数据库相关（高优先级）

| 迁移脚本 | 说明 | 状态 |
|---------|------|------|
| `2026-06-08-board-constituent-name.sql` | **新增** board_constituent 表增加 name 字段，从 stock_basic 回填 | ⚠️ 需执行 |
| `2026-05-30-float-to-decimal.sql` | **新增** 14 张表的 FLOAT→DECIMAL(18,4) 大迁移 | ⚠️ 需执行 |
| `2026-05-29-unify-collation.sql` | **新增** 统一 collation 为 utf8mb4_unicode_ci | ⚠️ 需执行 |
| `2026-05-28-add-visitor-log.sql` | **新增** visitor_log 访客日志表 | ⚠️ 需执行 |
| `2026-05-28-add-portfolio-trade-dividend.sql` | **新增** portfolio_trade 增加 dividend 字段 | ⚠️ 需执行 |
| `2026-05-25-add-performance-indexes.sql` | **已修改** CREATE INDEX→ALTER TABLE ALGORITHM=INPLACE, LOCK=NONE | ⚠️ 需重新执行 |
| `2026-05-25-dedup-stock-basic.sql` | **已修改** 兼容 MySQL 5.7 语法 | ⚠️ 需重新执行 |
| `2026-05-09-add-portfolio-daily-pnl.sql` | **已删除**（表被后续迁移 DROP，models.py 无对应模型） | 无需操作 |
| `2026-05-09-add-portfolio-daily-tables.sql` | **已修改** FLOAT→DECIMAL（已创建的表不受影响） | 如已执行则跳过 |

**⚠️ 远程已执行过的旧迁移**（无需重复）：
- `2026-05-07-add-portfolio-position-snapshot.sql`
- `2026-05-09-add-market-sentiment-tables.sql`
- `2026-05-16-add-board-tables.sql`
- `2026-05-20-add-system-log-tables.sql`

### API / 接口变更
- **板块路由** `/api/board/*` — 新增板块列表、详情、成分股等端点
- **同步接口** `/api/sync/*` — 新增板块/ETF、成分股同步端点
- **组合接口** `/api/portfolio/*` — 新增分红、日交易记录功能
- **同步 worker** — 异步任务逻辑重构，更稳定

### 前端 UI 变更
- **SyncJobLogView** — 板块状态卡片从独立区域改为侧边栏集成布局，新增最新交易日自动填充
- **BoardListView** — 板块列表页重构
- **ConfirmDialog** — 全局确认弹窗组件（共用）
- **PortfolioDayTradeModal** — 组合日交易弹窗（新增）

### 配置 / 部署
- **环境变量** — 无新增项
- **Nginx** — 无变更
- **Systemd** — 无变更
- **requirements.txt** — 无变更（无需重新 pip install）

---

## 部署检查项

- [ ] **数据库迁移**（共 6 个脚本待执行，见上方表格，注意执行顺序）
  - 优先执行 `2026-05-29-unify-collation.sql`（collation fix）
  - 其次 `2026-05-30-float-to-decimal.sql`（大表 ALTER，耗时长）
  - 最后执行其他迁移
- [ ] **前端 dist** — ✅ 已构建完成：`seele-frontend/dist/`
  - `chunk-vendors.js` (6.8MB) + `app.js` (1.7MB)
- [ ] **后端依赖** — 无需重新安装
- [ ] **Nginx** — 无变更，无需 reload
- [ ] **Systemd** — 无变更，无需 daemon-reload
- [ ] **重启后端** — 部署后必须重启

---

## 远程手动操作备忘

```bash
# 0. 备份数据库
mysqldump -u root -p seele > /tmp/seele_backup_v20.sql

# 1. 执行数据库迁移（按顺序）
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-05-29-unify-collation.sql
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-05-30-float-to-decimal.sql
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-05-28-add-visitor-log.sql
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-05-28-add-portfolio-trade-dividend.sql
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-05-25-dedup-stock-basic.sql
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-05-25-add-performance-indexes.sql
mysql -u root -p seele < /opt/seele/db-ops/migrations/2026-06-08-board-constituent-name.sql

# 2. 上传前端 dist/
rsync -avz --delete ./seele-frontend/dist/ root@8.138.165.208:/opt/seele/seele-frontend/dist/

# 3. 上传后端代码（全量同步，排除 node_modules/ .venv/ dist/ .git/）
rsync -avz --delete \
  --exclude='.venv' --exclude='node_modules' --exclude='dist' --exclude='.git' \
  ./ root@8.138.165.208:/opt/seele/

# 4. 重启后端
ssh root@8.138.165.208 "sudo systemctl restart seele-backend && sudo systemctl status seele-backend --no-pager"

# 5. 验证
ssh root@8.138.165.208 "curl -s http://127.0.0.1:9000/api/health"
```
